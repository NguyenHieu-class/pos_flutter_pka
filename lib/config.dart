/// Global configuration constants for the POS application.
///
/// [apiBase] should point to the root of the backend project (without the
/// version suffix). Use [apiVersionPath] to control which API version the
/// frontend talks to.
const String apiBase = 'http://10.0.2.2/pos_api';
const String apiVersionPath = '/v1';
